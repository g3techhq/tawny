# When omitted, Tawny stores SurrealDB data locally with the embedded RocksDB
# engine. Set this only when using a separate SurrealDB deployment.
SURREALDB_HOST=ws://127.0.0.1:8000
SURREALDB_NAMESPACE=tawny
SURREALDB_NAME=main
SURREALDB_USER=root
SURREALDB_PASSWORD=root

# Optional directory for embedded DB, extractor, and WebSub secret files.
TAWNY_DATA_DIR=C:/path/to/tawny-data

# Optional rustypipe-botguard executable. Enables PO-token-backed streams on
# YouTube client types that require BotGuard. Session PO tokens are cached.
TAWNY_BOTGUARD_BIN=C:/path/to/rustypipe-botguard.exe

# Path to a yt-dlp binary used to obtain stream URLs. Leave unset to use
# `yt-dlp` from PATH.
TAWNY_YTDLP_BIN=C:/path/to/yt-dlp.exe

# Optional bgutil HTTP service for yt-dlp's PO-token provider plugin. Current
# YouTube URLs can require a new video-bound token for every video; without one
# an apparently valid stream may later return 403. Port 4416 is the provider's
# default. Leave unset only when yt-dlp is already configured with a provider.
TAWNY_PO_TOKEN_PROVIDER_URL=http://127.0.0.1:4416

# Public HTTPS origin and optional explicit callback URL for YouTube WebSub.
# WebSub remains disabled on localhost; polling and manual refresh still work.
TAWNY_PUBLIC_URL=https://tawny.example
TAWNY_WEBSUB_CALLBACK_URL=https://tawny.example/api/v1/websub/youtube

# Persist this in deployment secrets. If omitted, Tawny generates and stores a
# random secret below TAWNY_DATA_DIR.
TAWNY_WEBSUB_SECRET=replace-with-a-random-64-character-secret

# Used by packaged desktop/mobile clients to reach the Tawny server.
SERVER_URL=http://127.0.0.1:8080
